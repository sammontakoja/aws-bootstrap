{
  "name": "aws-bootstrap",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "pm2 start server.js --name hello_aws --log ../logs/app.log ",
    "stop": "pm2 stop hello_aws",
    "build": "echo 'Building...'"
  }
}
